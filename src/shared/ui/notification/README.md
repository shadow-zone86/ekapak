# Notification

Компонент уведомлений с возможностью автоматического закрытия, закрытия кнопкой и визуальной полоской прогресса.

## 📋 Описание

Notification - это универсальный компонент для отображения уведомлений пользователю. Поддерживает различные типы уведомлений (info, success, warning, error), автоматическое закрытие с визуальной полоской прогресса и закрытие кнопкой.

## 🎯 Особенности

- **Типы уведомлений**: info, success, warning, error
- **Автоматическое закрытие**: с настраиваемой длительностью и визуальной полоской прогресса
- **Кнопка закрытия**: кнопка в правом верхнем углу
- **Адаптивность**: оптимизирован для мобильных устройств
- **Анимации**: плавные переходы и анимации

## 📦 Структура

```
notification/
├── ui/
│   └── notification.tsx         # Основной компонент
├── index.ts                      # Экспорт компонента
└── README.md                     # Документация
```

## 🔧 Использование

### Базовое использование

```tsx
import { Notification } from '@/shared/ui/notification';

function MyComponent() {
  const [isVisible, setIsVisible] = useState(true);

  if (!isVisible) return null;

  return (
    <Notification
      id="notification-1"
      title="Уведомление"
      message="Это базовое уведомление"
      onClose={(id) => {
        console.log('Закрыто:', id);
        setIsVisible(false);
      }}
    />
  );
}
```

### Различные типы уведомлений

```tsx
// Информационное уведомление
<Notification
  id="info-1"
  type="info"
  title="Информация"
  message="Информационное сообщение"
  onClose={handleClose}
/>

// Успешное уведомление
<Notification
  id="success-1"
  type="success"
  title="Успех"
  message="Операция выполнена успешно"
  onClose={handleClose}
/>

// Предупреждение
<Notification
  id="warning-1"
  type="warning"
  title="Предупреждение"
  message="Обратите внимание на это сообщение"
  onClose={handleClose}
/>

// Ошибка
<Notification
  id="error-1"
  type="error"
  title="Ошибка"
  message="Произошла ошибка при выполнении операции"
  onClose={handleClose}
/>
```

### Настройка автоматического закрытия

```tsx
<Notification
  id="notification-1"
  title="Автоматическое закрытие"
  message="Это уведомление закроется через 5 секунд"
  autoClose={true}
  duration={5000}
  onClose={handleClose}
/>
```

### Отключение автоматического закрытия

```tsx
<Notification
  id="notification-1"
  title="Постоянное уведомление"
  message="Это уведомление не закроется автоматически"
  autoClose={false}
  onClose={handleClose}
/>
```

### Без кнопки закрытия

```tsx
<Notification
  id="notification-1"
  title="Без кнопки закрытия"
  message="Это уведомление можно закрыть только автоматически"
  closable={false}
  autoClose={true}
  duration={5000}
  onClose={handleClose}
/>
```

## 📚 API

### Props

| Prop        | Тип                                         | По умолчанию | Описание                       |
| ----------- | ------------------------------------------- | ------------ | ------------------------------ |
| `id`        | `string`                                    | -            | Уникальный идентификатор (обязательный) |
| `title`     | `string`                                    | -            | Заголовок уведомления          |
| `message`   | `string`                                    | -            | Текст сообщения (обязательный) |
| `type`      | `'info' \| 'success' \| 'warning' \| 'error'` | `'info'`     | Тип уведомления                |
| `autoClose` | `boolean`                                   | `true`       | Автоматическое закрытие        |
| `duration`  | `number`                                    | `7000`       | Длительность в миллисекундах   |
| `closable`  | `boolean`                                   | `true`       | Показывать кнопку закрытия     |
| `onClose`   | `(id: string) => void`                      | -            | Callback при закрытии (обязательный) |

### Примеры типов

- **info**: Информационные сообщения (синий цвет)
- **success**: Успешные операции (зеленый цвет)
- **warning**: Предупреждения (оранжевый цвет)
- **error**: Ошибки (красный цвет)

## 🎨 Стилизация

Компонент использует Tailwind CSS классы для стилизации:

- Цвета границ и иконок зависят от типа уведомления
- Адаптивные размеры для мобильных устройств
- Плавные переходы и анимации

### Кастомизация

Компонент можно стилизовать через пропсы, но для более глубокой кастомизации потребуется модификация компонента.

## 📱 Адаптивность

Компонент адаптирован для мобильных устройств:

- Минимальная ширина: 320px
- Максимальная ширина: 480px
- Оптимизированные отступы для касаний

## ♿ Доступность

- Поддержка ARIA атрибутов (`aria-label` для кнопки закрытия)
- Семантическая разметка
- Контрастные цвета
- Фокус на интерактивных элементах

## 🧪 Тестирование

Компонент можно протестировать в Storybook, где представлены все варианты использования.

## 🚀 Примеры использования

### С NotificationProvider

Рекомендуется использовать компонент через `NotificationProvider` для глобального управления уведомлениями:

```tsx
import { NotificationProvider, useNotificationContext } from '@/shared/ui/notification-provider';

function MyComponent() {
  const { addNotification } = useNotificationContext();

  const handleClick = () => {
    addNotification({
      type: 'success',
      message: 'Операция выполнена успешно',
      duration: 3000,
    });
  };

  return <button onClick={handleClick}>Показать уведомление</button>;
}
```

### Прямое использование

```tsx
import { Notification } from '@/shared/ui/notification';

function MyComponent() {
  const [notifications, setNotifications] = useState([
    { id: '1', message: 'Первое уведомление', type: 'info' as const },
    { id: '2', message: 'Второе уведомление', type: 'success' as const },
  ]);

  return (
    <div className="flex flex-col gap-3">
      {notifications.map((notification) => (
        <Notification
          key={notification.id}
          {...notification}
          onClose={(id) => {
            setNotifications((prev) => prev.filter((n) => n.id !== id));
          }}
        />
      ))}
    </div>
  );
}
```

## 📚 Связанная документация

- [NotificationProvider](./notification-provider/README.md) - Провайдер для управления уведомлениями
- [Design System](../../../docs/design-system.md) - Система дизайна
